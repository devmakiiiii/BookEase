-- Create extensions if not exists
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- This script initializes the database schema
-- Run this in the Neon Console or use Prisma migrations
-- For this project, we're using Prisma ORM, so use: npx prisma migrate dev --name init
